{
  "name": "haninge-gravtjanst-website",
  "version": "1.0.0",
  "private": true,
  "description": "Haninge Grävtjänst AB — mark- och anläggningsarbete i Stockholm",
  "scripts": {
    "build": "node scripts/build.js",
    "start": "npm run build && npx --yes serve dist -l 3000"
  },
  "dependencies": {
    "resend": "latest"
  }
}
